version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: neuroimaging_postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-neuroimaging}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-neuroimaging}
      POSTGRES_DB: ${POSTGRES_DB:-neuroimaging}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-neuroimaging}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
